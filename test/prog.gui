// Declaracoes
let 
    integer n, x, z;
    integer a, b, c;
    float q, w, e;

/* Comandos */
in 
    read n;
    read x;

    write n;
    write x;

    while n x < do
        read x;
    end;

    a := n;
    b := b 1 +;

    if (a b =) then
        write a;
    else
        write b;
    end;

end
